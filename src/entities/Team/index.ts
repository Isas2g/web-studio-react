import {
  fetchAPITeams,
  createTeam,
  updateTeam,
  fetchTeam,
  fetchTeams,
} from './store';
import { Team } from './types';

export { fetchAPITeams, createTeam, updateTeam, fetchTeam, fetchTeams };

export type { Team };
